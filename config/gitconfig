[rebase]
  autostash=true
[push]
  default = simple
[alias]
  blog="log origin/master... --left-right"
  br=branch
  cane="duet-commit --amend --no-edit --reset-author"
  ci=duet-commit
  co=checkout
  dc=diff --cached
  di=diff
  ds="diff --staged"
  fetch="fetch --all --prune"
  fix-remote = ! "f() { export remote=$(git remote get-url origin --push); if [ -z ${remote##https://github.com/*} ]; then git remote set-url origin --push \"git@github.com:${remote#https://github.com/}\"; fi; unset remote; }; f"
  fixup="commit --fixup"
  flog="log --pretty=fuller --decorate"
  llog="log --date=local"
  lol="log --graph --decorate --oneline"
  lola="log --graph --decorate --oneline --all"
  p="pull --rebase --autostash"
  rum="rebase master@{u}"
  squash="commit --squash"
  st=status
  sta=stash
  sur="submodule update --init --recursive"
  unstage="reset HEAD"
[user]
	email = tpersen@pivotal.io
	name = Todd Persen
[duet "env"]
	git-author-initials = todd
	git-author-name = Todd Persen
	git-author-email = tpersen@pivotal.io
	mtime = 1531868156
	git-committer-initials = 
	git-committer-name = 
	git-committer-email = 
